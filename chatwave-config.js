window.CHATWAVE_CONFIG = {
  // Si tu frontend vive en GitHub Pages, pega aqui la URL completa del backend en Vercel.
  // Ejemplo: "https://tu-proyecto.vercel.app/api/chat"
  apiUrl: "",

  // Si OpenAI falla, la web puede seguir usando respuestas demo temporales.
  allowDemoFallback: true
};
